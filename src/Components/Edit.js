import ReactDOM from "react-dom/client";
import React from "react";
function Edit(id) {
    
        const x = ReactDOM.createRoot(document.getElementById(id));
              x.render(
                
                    )
    
}